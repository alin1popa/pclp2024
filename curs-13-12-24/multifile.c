#include <stdio.h>
#include "utils.h"
#include "utils.h"
#include "utils.h"
#include "utils.h"

int main() {
    int a = 5, b = 3;
    printf("a + b = %d\n", aduna(a, b));
    printf("a - b = %d\n", scade(a, b));
    return 0;
}